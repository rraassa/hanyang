services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile.static
    container_name: nambaone-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - nambaone-network

networks:
  nambaone-network:
    driver: bridge
