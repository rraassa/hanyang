services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile.static
    container_name: nambaone-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - nambaone-network

networks:
  nambaone-network:
    driver: bridge
